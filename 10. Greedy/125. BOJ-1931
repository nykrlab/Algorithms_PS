# 주어진 시작시간과 끝나는 시간들을 이용해서 가장 많은 회의 수 알기 위해서는 
# 빨리 끝나는 회의 순서대로 정렬해야함
# 빨리 끝날수록 뒤에서 고려해볼 회의가 많기 때문 
# 빨리 시작하는 순서대로 정렬한다면 오히려 늦게 끝날 수 있음 

# 끝나는 시간이 같을 경우, 빨리 시작하는 순서대로 정렬되어야 함
# 끝나는 시간 오름차순 / 시작시간 오름차순

import sys

n = int(sys.stdin.readline())
time=[[0]*2 for _ in range(n)] 

for i in range(n):
  s,e = map(int,sys.stdin.readline().split())
  time[i][0]=s
  time[i][1]=e
  
time.sort(key=lambda x:(x[1],x[0]))

cnt = 1
end_time = time[0][1] #첫 endtime
for i in range(1,n): #1~n-1
  if time[i][0]>=end_time: #i번째 starttime
    cnt+=1
    end_time=time[i][1]

print(cnt)
