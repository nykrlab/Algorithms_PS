r = 1000000

arr = [True for _ in range(r)]

for i in range(2,int(r**0.5)+1): #에라토스테네스의 체
  if arr[i]:
    for k in range(2*i,r,i):
        if arr[k]==True:
            arr[k]=False
# 소수에 해당하는 값만 true 가지고 있음
      
import sys 
input=sys.stdin.readline

while True:
  n=int(input())
  if n==0:break

  for i in range(3,len(arr)):
    if arr[i]==True:
      if arr[n-i]:
        print(n,'=',i,'+',n-i)
        break

    # i가 작은수부터 시작이므로 arr[i]와 arr[n-i]가 
    # 둘 다 arr에 존재할 경우, 해당 값 출력하고 바로 break 
    
    
    ## =_=... i와 n-i,, 제대로 쓰세요

