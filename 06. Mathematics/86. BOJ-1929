# 에라토스테네스의 체 - 배수 빼기

m,n = map(int,input().split())

def isprime(m,n):
  n+=1
  prime=[True]*(n)
  for i in range(2,int(n**0.5)+1): 
  # n의 제곱근까지만 검사
    if prime[i]: #prime[i] true일 때
      for j in range(2*i,n,i): 
      # prime 내 i 배수들을 false로 변환
        prime[j]=False
  for i in range(m,n):
    if i>1 and prime[i]==True:
      print(i)

isprime(m,n)



"""
m,n=map(int,input().split())

def isPrime(x):
  if(x<2): #소수 판별 함수
   return False #1은 소수 아니므로 false
  for i in range(2,x):
    if(x%i==0):
      return False
  return True

  for i in range(m,n+1):
    if(isPrime(i)):
      print(i)

# m과 n 사이 숫자 하나하나 확인
# >> 시간 초과
"""
