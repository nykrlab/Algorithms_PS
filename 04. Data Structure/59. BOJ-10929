#문자열 S가 주어졌을 때, SHA-224 해시값을 구하는 프로그램을 작성하시오.

# hash에는 많은 종류가 있다 SHA1 SHA224 SHA384 SHA512 등
# 각 종류마다 generate하는 값이 다른데, 문제에서 sha224를 원하므로 
# sha224 사용위해 위에 언급한 hash 함수 모두 포함하는 hashlib을 import
# hashlib.sha224()와 같은 방식으로 원하는 hash 함수 불러서 사용

import hashlib
print(hashlib.sha224(input().encode('utf-8')).hexdigest())
# incode를 해주지 않으면 TypeError난다 
# >> unicode-objects must be encoded before hashing
# input값 뒤에 .encode('utf-8') 이용해서 인코딩 바꿔줌


# m = hashblib.sha256()
# m.update('Life is too short'.encode('utf-8'))

# hashlib.sha256()로 생성한 객체 m에 해싱한 문자열을 입력으로 update 함수 호출하면 문자열 해싱됨
# update 함수에 전달하는 문자열은 바이트문자열이어야하므로 
#.encode('utf-8')과 같이 > 유니코드 문자열을 UTF-8 형식의 바이트 문자열로 변환

# m.digest() > 문자열을 해싱했다면 다음처럼 digest 
# 혹은
# m.hexdigest() > hexdigest 함수 사용하여 해싱된 문자열 얻을 수 있다

# digest()는 해싱된 바이트 문자열을 리턴하고 hexgigest()는 바이트 문자열을 16진수로 변환한 문자열을 리턴한다.
